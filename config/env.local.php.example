<?php
declare(strict_types=1);

// Copy this file to env.local.php (gitignored) and fill in real credentials.
// env.local.php is loaded before database.php if it exists.

putenv('DB_HOST=127.0.0.1');
putenv('DB_NAME=partambus');
putenv('DB_USER=root');
putenv('DB_PASS=');
